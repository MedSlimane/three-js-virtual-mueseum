import React from 'react';
import { Html } from '@react-three/drei';

const Loading: React.FC = () => (
  <Html center>
    <div className="loading">
      Loading museum environment...
    </div>
  </Html>
);

export default Loading;